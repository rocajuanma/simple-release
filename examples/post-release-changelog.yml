# Example: Post-release changelog workflow
# Copy this file to your repository as .github/workflows/post-release-changelog.yml

name: Post-Release Changelog Update

on:
  release:
    types: [published]  # Triggers after a release is successfully published

jobs:
  update-changelog:
    uses: rocajuanma/simple-release/.github/workflows/reusable-post-release-changelog.yml@v1.0.0
    with:
      changelog-path: 'CHANGELOG.md'        # Optional: Changelog path (default: CHANGELOG.md)
      base-branch: 'main'                   # Optional: Base branch for PR (default: main)
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}